Function ArrayRowContains(inputArray As Variant, idx As Long, Contains As String)
    'mind if inputarray is 0 or 1 base when passing idx
    Dim r As Long, c As Long
    For c = LBound(inputArray, 2) To UBound(inputArray, 2)
        If inputArray(idx, c) Like Contains Then ArrayRowContains = True: Exit Function
    Next
End Function
